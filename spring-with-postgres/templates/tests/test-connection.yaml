apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-with-postgres.fullname" . }}-test-connection"
  labels:
{{ include "spring-with-postgres.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "spring-with-postgres.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
